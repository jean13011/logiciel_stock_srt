{% extends 'base.html.twig' %}

{% block title %}Connection{% endblock %}

{% block body %}

    <div class="form-group  d-flex justify-content-center bg-white p-2 rounded-top container mt-5 p-5 w-25 h-75 shadow-lg p-3 mb-5 bg-white rounded">
        {{form_start(form, {"attr" : {"class" : "was-validated"}})}}
            <h1 class="text-center"> <strong>Bienvenue</strong> </h1>
            {{form_errors(form)}}
            <div class="form-group">
                {{form_row(form.Nom_utilisateur)}}
                {{form_errors(form.Nom_utilisateur)}}
            </div>
            <div class="form-group">
                {{form_row(form.password)}}
                {{form_errors(form.password)}}
            </div>
            <br><br>
            <button type="submit"  class="btn btn-primary justify-content-center w-100 p-3">connection</button>
            {{ form_rest(form) }}
        {{form_end(form)}}
    </div>
{% endblock %}

